{
  "dependencies": {
    "@better-auth/expo": "catalog:",
    "@convex-dev/better-auth": "catalog:",
    "@edge-runtime/vm": "^5.0.0",
    "@mybeachapp/shared": "workspace:*",
    "better-auth": "catalog:",
    "convex": "catalog:",
    "convex-test": "^0.0.51",
    "kysely": "catalog:"
  },
  "devDependencies": {
    "@mybeachapp/config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "name": "@mybeachapp/backend",
  "private": true,
  "scripts": {
    "build": "convex codegen --dry-run --typecheck enable",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "clear": "rm -rf .turbo dist *.tsbuildinfo",
    "dev": "convex dev",
    "test": "vitest run",
    "typecheck": "bun run build"
  },
  "type": "module",
  "version": "0.0.0"
}
